# T-Efficient-AI-Project.
Final Project Code 
## Requirements
1. Python 3.9+
2. pandas
3. matplotlib
4. psutil

## Run Experiments
Baseline:
python -m src.benchmark --mode baseline

Laptop sweep:
python -m src.benchmark --mode laptop_sweep

Phone sweep:
python -m src.benchmark --mode phone_sweep

## Results
Outputs are saved in results folder
